# Email Service Configuration
server.port=8086
spring.application.name=email-service

# MongoDB Configuration (shared with other services)
spring.data.mongodb.uri=mongodb+srv://harshiniv218_db_user:6AsUAawu99sNKTC8@cluster0.2zmjrjl.mongodb.net/ProfConnect?retryWrites=true&w=majority
spring.data.mongodb.database=ProfConnect

# Email Configuration
# Primary: Gmail API (via Google Cloud OAuth2)
# Fallback: SMTP (if Gmail API not authenticated)



# SMTP Configuration (Fallback - requires App Password if Gmail API not used)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:profconnect11@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vyld ymfk cloc rbjn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

